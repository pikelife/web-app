module.exports = class Index

    constructor: (opts={}) ->
        {@label, @property} = opts

    equals: (other) ->
        (other instanceof Index) and
            (other.label is @label) and
            (other.property is @property)

    toString: ->
        "INDEX ON :#{@label}(#{@property})"     # E.g. INDEX ON :User(email)
